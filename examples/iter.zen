mod stdlib;

fn iter arr func {
	let arr = clone(arr);
	while array_size(arr) > 0 {
		if func(arr[0]) == false {
			return null;
		}

		let arr = array_remove(arr, 0);
	}
	return null;
}

fn main {
	iter([1,2,3,4,5], fn item {
		println(item);
		return true;
	});
	return null;
}
