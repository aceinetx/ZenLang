mod stdlib;

fn iter arr func {
	while array_size(arr) > 0 {
		if func(arr[0]) == false {
			return null;
		}

		let arr = array_remove(arr, 0);
	}
	return null;
}

fn print_item item {
	println(item);
	return true;
}

fn main {
	iter([1,2,3,4,5], print_item);
	return null;
}
